Directory Enumeration Tool.

Enumeration:

Discover resources that aren't necessarily visible from the application's main page.

Install

sudo apt install dirb -y

Check wordlists

ls /usr/share/wordlists/dirb/

Remaining steps refer the report that also contains screenshot.
